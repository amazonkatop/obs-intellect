---
interface Props {
  h2: string;
  text: string;
  primaryHref: string;
  primaryLabel: string;
  secondaryHref?: string;
  secondaryLabel?: string;
  note?: string;
}

const { h2, text, primaryHref, primaryLabel, secondaryHref, secondaryLabel, note } = Astro.props;
---

<section class="border-t border-line">
  <div class="wrap py-20 md:py-24">
    <h2 class="page-h2 max-w-3xl">{h2}</h2>
    <p class="mt-5 max-w-2xl text-base leading-7 text-muted md:text-lg md:leading-8">{text}</p>
    <div class="mt-8 flex flex-wrap items-center gap-3">
      <a href={primaryHref} class="btn-primary">{primaryLabel}</a>
      {
        secondaryHref && secondaryLabel && (
          <a href={secondaryHref} class="btn-secondary">
            {secondaryLabel}
          </a>
        )
      }
    </div>
    {note && <p class="mt-6 max-w-2xl text-base leading-7 text-muted">{note}</p>}
  </div>
</section>
